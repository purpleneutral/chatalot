# Chatalot deployment configuration
# Copy this file and fill in your values:
#   cp scripts/deploy.env.example scripts/deploy.env
#
# Then source it before running deploy.sh:
#   source scripts/deploy.env
#   ./scripts/deploy.sh --pull-only
#
# ── Deployment Workflow ──────────────────────────────────────────────
#
#   1. Edit code on your development workstation
#   2. Commit and push to your staging Git remote (e.g. self-hosted Forgejo)
#   3. Deploy to local/staging server — test thoroughly
#   4. Once verified, push to your production Git remote (e.g. GitHub)
#   5. Deploy to public production server
#
# ── Staging (local prod) ────────────────────────────────────────────
#
# DEPLOY_HOST=user@staging-server
# DEPLOY_DIR=/srv/chatalot
# DEPLOY_GIT_URL=ssh://git@your-git-server/you/chatalot.git
# DEPLOY_DOMAIN=chat.local
# DEPLOY_NETWORK=web
#
# ── Production (public) ─────────────────────────────────────────────
#
# DEPLOY_HOST=user@production-server
# DEPLOY_DIR=/srv/chatalot
# DEPLOY_GIT_URL=https://github.com/you/chatalot.git
# DEPLOY_DOMAIN=chat.example.com
# DEPLOY_NETWORK=web

# Uncomment and fill in ONE environment at a time:
DEPLOY_HOST=
DEPLOY_DIR=
DEPLOY_GIT_URL=
# DEPLOY_DOMAIN=     # Optional: for Traefik reverse proxy auto-config
# DEPLOY_NETWORK=    # Optional: Docker network for Traefik (default: web)
